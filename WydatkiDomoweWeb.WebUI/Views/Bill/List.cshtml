@using WydatkiDomoweWeb.Domain.Entities
@model WydatkiDomoweWeb.WebUI.Models.BillListViewModel


@{
    ViewBag.Title = "Rachunki";
}

<div class="panel panel-default">
    <div class="panel panel-body">
        @Html.Action("Checkbox", "Checkbox")
    </div>
    <div class="panel-body">
        <table class="table table-striped table-condensed table-bordered">
            <tr>
                <th>Rachunek</th>
                <th>Odbiorca</th>
                <th class="NumericCol">Cena</th>
                <th>Data wpłaty</th>
                <th>Termin zapłaty</th>
                <th>Akcja</th>
            </tr>
            @foreach (var b in Model.Bills)
            {
                <tr>
                    <td>@b.BillName</td>
                    <td>@b.Recipient</td>
                    <td>@b.Amount</td>
                    <td>@b.PaymentDate</td>
                    <td>@b.RequiredDate</td>
                    <td>
                        @using (Html.BeginForm("Delete", "Admin"))
                        {
                            <input type="submit" value="Usuń" />
                        }
                    </td>
                </tr>
            }
        </table>
        <div class="btn-group pull-right">
            @Html.PageLinks(Model.PagingInfo, x => Url.Action("List", new { page = x }))
        </div>
    </div>

    <div class="panel-footer">
        @Html.ActionLink("Dodaj nowy produkt", "Usuń", null, new { @class = "btn btn-default" })
    </div>
</div>

